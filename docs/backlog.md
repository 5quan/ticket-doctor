# 可优化清单（Backlog）

状态：`待办` / `进行中` / `已完成` / `暂缓`。优先级：P0 最高。

## 一、飞书端触发的问题

| # | 问题 | 现状 / 决策 | 建议 | 状态 | 优先级 |
|---|---|---|---|---|---|
| F1 | 引用（回复）不一定返回 `root_id` | 平台行为，无法控制；`root_id` 缺失时线程路由失效 | **文档明确推荐使用标号 `[TD-xxxxxxxx]`**；交互上引导用户回复时保留标号 | 待办 | P1 |
| F2 | 标号与引用带来的 id 冲突 | 已确认产品决策：**以标号为主**（当前实现即标号无条件优先） | 补单测锁定该语义，避免后续重构被改掉；在文档中写明冲突行为 | 待办 | P1 |
| F3 | 展示格式需要按体验再优化 | 当前报告信息密度高、偏工程化（长文本） | 缩短摘要、事实/假设用卡片或折叠、只对“缺失材料/待验证”高亮；飞书卡片替代纯文本 | 待办 | P2 |

## 二、可观测性与审计（真实循环）

| # | 问题 | 现状 | 建议 | 状态 | 优先级 |
|---|---|---|---|---|---|
| O1 | 逐次工具调用未落盘 | `run_events` 只有 `run_started / engine_finished / report_saved / run_error`，没有每次工具的入参、耗时、成功失败 | 增加 `tool_started / tool_completed / observation_added` 事件（方案原定要求） | 待办 | P1 |
| O2 | 模型对话未落盘 | pi 会话使用内存 `SessionManager`，run 结束即 `dispose`；system/user/assistant/tool 消息不可回放 | 若要审计模型推理，需落盘对话或启用 pi 会话文件（见 R2） | 待办 | P2 |
| O3 | token / 成本用量未记录 | 只记录了 `toolCalls / modelTurns / model`，没有 token 数 | 从 pi 的 usage 汇总写入 `engine_finished`，并纳入预算 | 待办 | P1 |
| O4 | 进度未反馈 | 方案要求“已接收 / 开始执行 / 最终结果” | 目前只在最终报告回复；可补“已接收/开始执行”两条轻量回复 | 待办 | P2 |

## 三、安全与合规

| # | 问题 | 现状 | 建议 | 状态 | 优先级 |
|---|---|---|---|---|---|
| S1 | 日志/源码未脱敏 | 工具输出原样进模型与 `evidence` 表，**没有脱敏逻辑** | 在工具边界做脱敏（手机号/邮箱/token/身份证/密钥），落盘前生效 | 待办 | P1 |
| S2 | 工具结果总大小未单独设限 | 仅“每条证据”按 `maxResultChars` 截断；`read_code` 最多 200 行可能整体很大 | 增加“单次工具返回总字符数”上限 | 待办 | P2 |

## 四、材料源

| # | 问题 | 现状 | 建议 | 状态 | 优先级 |
|---|---|---|---|---|---|
| M1 | 真实日志平台未接入 | 只有本地文件 `FileLogSource` | 按端口新增 SLS/ELK 适配器，不动编排 | 待办（用户明确不急） | P2 |

## 五、可靠性与调度

| # | 问题 | 现状 / 决策 | 状态 |
|---|---|---|---|
| R1 | 出站消息映射（回复机器人 → 归入原调查） | 当前依赖 `root_id`；未做 `provider_message_id → investigation` 反查 | **暂缓（用户决定不做）** |
| R2 | pi 会话文件持久化 | 有意不启用：pi 会话会保存完整工具输出，与 `evidence` 重复且含敏感内容；当前用 `contextSummary` 传递多轮 | 暂缓（按需再说） |

## 六、工程与体验

| # | 问题 | 建议 | 状态 |
|---|---|---|---|
| E1 | 无管理前端 | 保留 CLI/SQL 排查即可；必要时加只读查看页 | 待办 |
| E2 | 报告时间显示为 UTC | 展示改为本地时区（+08:00），减少阅读成本 | 待办 |
| E3 | 验收未脚本化 | 把方案第 11 节的验收写成 `npm run accept` 可运行断言 | 待办 |
